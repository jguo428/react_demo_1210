- pipeline: pipeline_jason_1210
  name: Pipeline
  refs:
  - refs/heads/main
  fail_on_prepare_env_warning: true
  resources: NANO
  events:
  - type: WEBHOOK
  actions:
  - action: npm run start
    type: BUILD
    docker_image_name: library/node
    docker_image_tag: 22
    execute_commands:
    - "# yarn install"
    - npm install
    - cd ./server
    - npm install
    - npm run start
    shell: BASH
